<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.project1v2mymoviescollection.Activities.SplashScreenActivity"
    android:background="#000000">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/imglogo"
        android:src="@drawable/movies_icon"
        android:scaleType="fitCenter"
        android:contentDescription="@string/splashscreen_image" />

    <TextView
        android:text="@string/my_movies_collection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textView2"
        android:textColor="#ffffff"
        android:textSize="36sp"
        android:gravity="center"
        android:layout_marginTop="30dp" />

    <ProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="20dp"
        android:progressDrawable="@android:color/holo_orange_light"
        android:visibility="visible" />

</FrameLayout>
